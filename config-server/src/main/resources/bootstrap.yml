spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/duninvit/kafka-demo-config-server-repository
          username: duninvit
          password:
          clone-on-start: true
          default-label: master
      fail-fast: true
